-- ============================================================
-- 모든 계좌 금액 초기화 (표시 잔액 전부 0원으로)
-- Supabase 대시보드 > SQL Editor에서 실행하세요
-- 주의: 이 작업은 되돌릴 수 없습니다. 실행 전 반드시 앱의
-- 설정 > 데이터 내보내기(JSON)로 백업을 먼저 받으세요.
-- ============================================================

-- ─── 모든 거래 내역 삭제 ───
DELETE FROM transactions;

-- ─── 모든 계좌의 초기 잔액을 0으로 초기화 ───
UPDATE accounts SET initial_balance = 0;
